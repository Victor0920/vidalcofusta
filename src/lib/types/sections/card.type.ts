export type CardType = {
	title?: string;
	description?: string;
	buttonText?: string;
	url: string;
	imageUrl?: string;
	imageDescription?: string;
};

export type CardPropertiesType = {
	columns?: number;
};
